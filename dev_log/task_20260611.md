# 작업 계획 및 진행 현황 (2026-06-11)

- [x] `wht_dynamic_solver.py` 수정: `extract_esl_advanced`에서 과구속 bcs 대신 등가 외력(forces) 인가 방식으로 전환
- [x] `solver.py` 수정:
  - [x] `get_C0_safe` 함수에 동적 ESL 명칭 패턴 매칭을 적용하여 정규화 강건성 확보
  - [x] Iter 0 모달 해석에 `try-finally` 도입 및 `exclude_rigid_body=False`, `shift_hz=0.0` 설정
  - [x] 루프 내 모달 해석에 `try-finally` 도입 및 `exclude_rigid_body=False`, `shift_hz=0.0` 설정
- [x] 수치 발산 해결을 위한 ESL 경계조건 재설정 및 RBE3 보존:
  - [x] 3-2-1 구속은 0.6mm 박판에 국부 우그러짐을 유발하므로 폐기
  - [x] 최적화 과정 동안 동해석용 임시 마스터 노드(#900000~#900003) 및 RBE3 강성을 그대로 보존
  - [x] `run_topo.py` 의 `_build_snapshots` 메서드에서 마스터 노드를 SPC로 Z, Y, Z (0, 1, 2) 구속
  - [x] `run_topo.py` 의 `_build_element_peak_lc` 메서드에서 마스터 노드를 SPC로 Z, Y, Z (0, 1, 2) 구속
  - [x] 최종 결과 내보내기(`_export`, `_run_optimizer` Design 1/2 루프 끝) 직전에 마스터 노드와 RBE3를 안전하게 제거하는 기능 구현
- [x] Optimization Monitor Settings 탭 테이블 복원 및 전달 최적화:
  - [x] `solver.py` 의 `_init_snap`에 `run_settings` 추가
  - [x] `monitor_ui.py` 의 `_restore_history`에서 `init.pkl` 로드 시 Settings 테이블 갱신 추가
- [/] 전체 최적화 파이프라인 작동성 검증 및 수치 분석:
  - [/] `run_topo.py` 테스트 실행 및 수치 수렴성 확인 (task-812)
  - [ ] 1 이터레이션 이후의 ESL 변위/응력/변형 에너지가 수 mm, 수백 MPa 수준으로 안착하여 테이블에 기록되는지 확인
  - [ ] 최종 비드 패턴 형상과 복구 시의 경계조건 무결성 검증
