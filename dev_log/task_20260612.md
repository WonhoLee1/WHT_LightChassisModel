# Optimization Monitor UI 개선 및 버그 재점검 작업

- [x] 1. Summary Table 2개 분할 (고정 열 구현)
  - [x] `monitor_ui.py` 의 `_build_tab_summary` 수정 (QTableWidget 2개 분할)
  - [x] `_append_summary_row` 및 `_summary_add_iter_col` 데이터 삽입 로직 변경
  - [x] 수직 스크롤바 동기화 로직 추가
- [x] 2. Iteration Results 레이아웃 개편 및 글자색 수정
  - [x] `monitor_ui.py` 의 `_build_tab_height` 레이아웃 구조 변경 (상-중-하)
  - [x] 왼쪽 리스트 최소 폭 154로 설정
  - [x] 텍스트 박스 글자색 기본색으로 변경
- [x] 3. 모달 해석 num_modes 수정
  - [x] `wht_topo/solver.py` 에서 `num_modes=self.modal_modes + 6` 을 `self.modal_modes` 로 수정
  - [x] `monitor_ui.py` 의 `BeadConceptEvalWorker` 에서 20 하드코딩된 값을 `num_modal_modes` 인자로 받아 처리하도록 수정
- [x] 4. 콤보 및 리스트 정렬 변경
  - [x] `monitor_ui.py` 에서 Latest를 제거하고 완전한 0~N 순방향 및 1:1 슬라이더/인덱스 동기화 구현
- [x] 5. Bead Concept Tool 개선
  - [x] `monitor_ui.py` 의 `BeadConceptDialog._refresh_canvas` 에서 legend 영역 삭제
  - [x] `BeadConceptEvalWorker` 의 `to_wht_result_data` 호출 시 잘못된 import 제거 (Visualizer 연동)
  - [x] `BeadConceptDialog` 에 `num_modal_modes` 전달 및 Worker 초기화 시 적용
- [x] 6. Modal Analysis 테이블 강체 모드 표시 추가 및 재구축(rebuild) 로직 재검점
  - [x] `_rebuild_history_ui` 와 `update_history` 의 모달 테이블 그리기 및 RBody Modes (6) 삽입 로직 정합성 일치
  - [x] `_get_snap` 인덱싱 버그 전면 검토 및 순방향 1:1 매핑 정교화
