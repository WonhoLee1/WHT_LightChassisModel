# WHT LightChassisModel — 변경 이력 (2026-06-19)

> **목적:** LLM이 다음 세션에서 컨텍스트 없이 읽어도 현재 코드 상태와 미해결 과제를 파악할 수 있도록 작성.

---

## 프로젝트 개요

Python 기반 경량 쉘 FEM 솔버.  
MITC4+ flat shell 요소 (QUAD4) + TRIA3 CST. 고유진동수 해석(모달) 및 정적 해석 지원.  
백엔드: JAX (vmap/jit), NumPy, Numba (nopython JIT).

```
wht_solver/
  wht_solver.py               -- 조립기, 경계조건, 모달 해석
  wht_quad4_element_jax.py    -- JAX 요소 강성 (레퍼런스 구현)
  wht_quad4_element.py        -- NumPy + Numba 요소 강성
SolverVerification/
  patch_tests.py              -- 22개 패치테스트 + 50×50 벤치마크
  test_fold_hinge.py          -- fold-line hinge spring 이론 검증
  compare_fold_hinge_beaded.py -- ccx_iter016 실패 패널 vs CalculiX + 속도비교
  ccx_iter016_Modal_Analysis_mesh.inp  -- 비드 패널 메시 (CalculiX 위상최적화 결과)
scratch/
  optimize_cdrill.py          -- C_drill 이분탐색 스크립트 (이력 코멘트 참조)
```

---

## 2026-06-19 변경 사항

### 1. Numba P_proj 3D 좌표 버그 수정 ★ 핵심

**파일:** `wht_solver/wht_quad4_element.py`  
**함수:** `_element_K_mitc4_plus_nb` (줄 ~489)

**원인:**  
Rigid Body Projection 행렬 R_rbm을 구성할 때,
- JAX/NumPy: `coords_loc = (coords - coords[0]) @ T_mat.T` → 3D local frame, **pz 포함**
- Numba (버그): `coords_2d` (2D 투영 좌표) → **pz = 0 강제**

Flat mesh(패치테스트)는 pz ≈ 0이므로 차이 없음 → 테스트 통과.  
실제 비드 패널처럼 warped element가 있는 3D 메시에서만 오차 발생.

**영향 (ccx_iter016 Mode 7):**

| 백엔드 | 수정 전 | 수정 후 | CCX 기준 |
|--------|---------|---------|---------|
| JAX    | 4.9376 Hz | 4.9376 Hz | +0.07% (항상 올바름) |
| Numba  | **6.9980 Hz** | **4.9376 Hz** | +42% → **+0.07%** |

**수정 내용:**
```python
# 수정 전 — coords_2d (pz=0)
px = coords_2d[i, 0]
py = coords_2d[i, 1]
R_rbm[6*i+0, 5] = -py        # pz 항 없음
R_rbm[6*i+1, 5] = px         # pz 항 없음

# 수정 후 — 3D local coords (pz = dot(p_i, z_loc))
coords_pz = np.zeros(4)
coords_pz[1] = p2[0]*z_loc[0] + p2[1]*z_loc[1] + p2[2]*z_loc[2]
coords_pz[2] = p3[0]*z_loc[0] + p3[1]*z_loc[1] + p3[2]*z_loc[2]
coords_pz[3] = p4[0]*z_loc[0] + p4[1]*z_loc[1] + p4[2]*z_loc[2]
...
pz = coords_pz[i]
R_rbm[6*i+0, 4] = pz;  R_rbm[6*i+0, 5] = -py   # pz 추가
R_rbm[6*i+1, 3] = -pz; R_rbm[6*i+1, 5] = px    # pz 추가
```

**검증:** 패치테스트 22/22 PASS (regression 없음), ccx_iter016 Mode 7 오차 +42% → +0.07%.

---

### 2. `_add_fold_hinge_stiffness` 구현

**파일:** `wht_solver/wht_solver.py`

fold-line(비드 능선) 힌지 스프링을 강성 행렬에 추가하는 메서드.  
`WHTSolver(model, fold_alpha=0.5, fold_phi_min_deg=3.0)` 으로 활성화.

**수식:** `k = alpha * D_avg * sin²(φ)` [N·mm/rad] (공유 엣지당)  
D_avg = E·t³/(12(1-ν²)), φ = 이면각  
6×6 비틀림 보 강성으로 엣지 노드 [rx,ry,rz] DOF에 조립.

**V-beam 이론 검증 결과 (`test_fold_hinge.py`):**

| 조건 | 이론 f₁ | WHT (spring 없음) | WHT (α=0.5) |
|------|---------|-----------------|------------|
| 평판 (φ=0°) | 0 Hz | 0 Hz | 0 Hz |
| L-section (φ=90°) | 518.35 Hz | 504.81 Hz (−2.61%) | **517.52 Hz (−0.16%)** |

**비드 패널(ccx_iter016)에서의 효과:**  
MAE 11.60% → 11.60% (변화 없음). 비드 패널에서는 fold spring 효과 미미.

---

### 3. 패치테스트 벤치마크 추가

**파일:** `SolverVerification/patch_tests.py`

50×50 QUAD4 (2,601 nodes / 2,500 elems / ~15,606 DOF) 모달 해석 속도 측정 추가.

| Backend | K [ms] | Modal [ms] | Total [ms] | vs Numba |
|---------|--------|------------|------------|---------|
| JAX     | 1,935  | 2,111      | 4,045      | 1.60×   |
| NumPy   | 2,922  | 4,383      | 7,305      | 2.89×   |
| **Numba** | **191** | **2,337** | **2,528** | **1.00×** |

Numba K assembly: NumPy 대비 **15×** 빠름.

---

### 4. 비드 패널 비교 스크립트

**파일:** `SolverVerification/compare_fold_hinge_beaded.py` (신규)

ccx_iter016 (4,109 nodes / 3,760 elems / 24,654 DOF) — CalculiX 대비 모달 비교.

**Numba P_proj 수정 후 결과 (C_drill=24.9766):**

| Mode | CCX [Hz] | WHT [Hz] | 오차 |
|------|----------|----------|------|
| 7    | 4.934    | 4.938    | +0.07% ✓ |
| 8    | 15.803   | 15.248   | −3.51% |
| 9    | 21.933   | 19.772   | −9.85% |
| 10–13 | —      | —        | ±5% 이내 |
| 14–20 | —      | —        | −15 ~ −27% ⚠ |

MAE: **11.60%**, Max: **26.98%**

> Mode 14 이후 WHT < CCX (15~27%)는 모드 번호 매칭 오류 가능성 높음.  
> WHT 스펙트럼에 42~45 Hz 구간 모드가 CCX보다 밀집되어 있어 이후 모드가 밀림.  
> 정확한 판단을 위해 MAC(Modal Assurance Criterion) 분석 필요.

**비드 패널 백엔드 속도 (Numba P_proj 수정 후):**

| Backend | K [ms] | Modal [ms] | Total [ms] | vs Numba |
|---------|--------|------------|------------|---------|
| JAX     | 2,040  | 4,695      | 6,735      | 1.48×   |
| NumPy   | 3,877  | 7,742      | 11,619     | 2.55×   |
| **Numba** | **306** | **4,251** | **4,558** | **1.00×** |

---

### 5. 표준 C_drill 값 조사

각 FEM 코드의 drilling DOF 강성 계수 현황:

| 코드 | 값 | 비고 |
|------|----|----|
| Hughes & Brezzi (1989) 원저 | c₁ = O(1) | 자유 파라미터 |
| Ibrahimbegovic et al. (1990) | **1/3 ≈ 0.333** | 학술 표준 |
| OpenSees ASDShellQ4 | **0.233** | `0.233 × G × t` |
| CalculiX S4 | **없음** | MPC 방식, drilling stiffness 미적용 |
| MSC Nastran K6ROT | 100 | 정규화 다름 |
| WHT 현재 | **24.9766** | CCX Mode 7 교정값 |

**시사점:**
- CalculiX S4는 drilling stiffness가 없음 → WHT C_drill=24.9766은 CCX가 아닌 다른 물리를 보상 중
- C_drill=1.0 적용 시 Mode 7 = 3.855 Hz (CCX 대비 −22%)
- C_drill=24.9766은 학술 표준 대비 ~75× 높음 — 이 값이 비드 구조 강성 보정을 일부 담당하는 것으로 추정

---

## 현재 코드 상태 (2026-06-19 기준)

### 확정된 사항

| 항목 | 상태 |
|------|------|
| 패치테스트 22/22 | ✅ PASS (세 백엔드 동일) |
| JAX ↔ NumPy ↔ Numba 수치 일치 | ✅ 0.0000% 차이 |
| Flat mesh (패치테스트) 정확도 | ✅ 이론 대비 ~2% 이내 |
| ccx_iter016 Mode 7 vs CCX | ✅ +0.07% |
| Numba 속도 (K assembly) | ✅ NumPy 대비 12~15× |

### 미해결 / 미고려 사항

| 항목 | 내용 |
|------|------|
| **비드 구조 강성 보정** | ⚠ 현재 미고려. Mode 7 이외 고차 모드에서 WHT와 CCX 간 불일치(-10~27%) 잔존. 비드(bead) 구조는 면내 강성을 국부적으로 높이므로, 단순 평판 MITC4 공식으로는 이 효과를 포착 불가. |
| C_drill 표준화 | ⚠ 현재값 24.9766은 CCX Mode 7 교정값. 표준값(1/3, 0.233)으로 변경 시 전체 주파수 ~22% 감소. OptiStruct 비교값 확보 후 재교정 필요. |
| 모드 번호 매칭 | ⚠ 고차 모드(14+)에서 MAC 없이 순번 매칭 중. WHT 스펙트럼과 CCX 스펙트럼의 모드 순서가 다를 가능성 있음. |
| OptiStruct 기준 주파수 | ⚠ 미확보. 과거 경험치 "WHT가 OptiStruct 대비 25~30% 과강성"은 Numba P_proj 버그 기인 가능성 높음. 수정 후 재비교 필요. |
| fold_alpha 효과 (비드 패널) | ⚠ 단순 V-beam에서는 효과 있으나, 실제 비드 패널에서는 효과 미미. 비드 구조에서의 hinge 모델링은 별도 검토 필요. |

---

## 향후 과제

1. **OptiStruct 기준 주파수 확보** → Numba P_proj 수정 후 WHT와 재비교
2. **C_drill 표준값 시험** → C_drill = 1/3 적용 후 CCX 및 OptiStruct 대비 전 모드 비교
3. **MAC 분석 구현** → 고차 모드 번호 매칭 신뢰도 확보
4. **비드 구조 강성 보정 방법 검토** → MITC4+ 요소 자체의 한계인지, 파라미터 보정으로 해결 가능한지 판단

---

## 파일별 변경 이력

| 파일 | 변경 내용 |
|------|---------|
| `wht_solver/wht_quad4_element.py` | Numba `_element_K_mitc4_plus_nb`: R_rbm 3D coords(pz) 추가 |
| `wht_solver/wht_solver.py` | `fold_alpha`, `fold_phi_min_deg` 파라미터 추가; `_add_fold_hinge_stiffness` 구현 |
| `SolverVerification/patch_tests.py` | `run_benchmark()` 함수 추가 (50×50 K+Modal 속도 측정) |
| `SolverVerification/test_fold_hinge.py` | 신규: V-beam 이론 검증 3종 |
| `SolverVerification/compare_fold_hinge_beaded.py` | 신규: ccx_iter016 fold-hinge 전후 비교 + 백엔드 속도비교 |
| `SolverVerification/results/patch_test_backend_comparison_20260619_rev2.md` | 신규: 전체 테스트 결과 보고서 |
| `CHANGES_20260619.md` | 신규: 본 문서 |
